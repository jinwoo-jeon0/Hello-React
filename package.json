{
  "name": "hello-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "^1.1.4",
    "serve": "^9.3.0"
  },
  "scripts": {
    "start": "concurrently \"yarn react-scripts start\" \"yarn server\"",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "server": "cd hello-express && node index.js"
  },
  "devDependencies": {
    "concurrently": "^3.6.1"
  }
}
